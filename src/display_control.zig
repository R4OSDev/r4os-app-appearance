//! Existing mode UI facade over the shared userland mode owner.
const modes = @import("r4gfx_desktop_outputs").modes;
pub const Controller = modes.Controller;
pub const confirmation_ms = modes.confirmation_ms;
pub const bars = modes.bars;
pub const fillPattern = modes.fillPattern;
